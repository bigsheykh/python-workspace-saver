(1, 'This is a bugfix release for bugs and regressions reported following the 1.15.1')

(1, '* The new cached allocations machinery has been fixed to be thread safe.')

(1, '* A small memory leak in PyArray_AdaptFlexibleDType has been fixed.')

(1, 'linked with OpenBLAS v0.3.0, which should fix some of the linalg problems')

(1, '* `11981 <https://github.com/numpy/numpy/pull/11981>`__: BUG: fix cached allocations without the GIL for 1.15.x')

(1, '* `11982 <https://github.com/numpy/numpy/pull/11982>`__: BUG: fix refcount leak in PyArray_AdaptFlexibleDType')

(3, '    2de0167b4297d1732e25c9288bbe3add  numpy-1.15.2-cp37-none-win32.whl')

