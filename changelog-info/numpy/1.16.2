(1, 'NumPy 1.16.2 is a quick release fixing several problems encountered on Windows.')

(3, 'addressed are:')

(1, 'There is also a regression fix correcting signed zeros produced by divmod, see')

(1, 'common on Debian derived distributions due to a modified pip.  The fix is to')

(1, '* `12909 <https://github.com/numpy/numpy/pull/12909>`__: TST: fix vmImage dispatch in Azure')

(1, '* `13020 <https://github.com/numpy/numpy/pull/13020>`__: BUG: fix signed zero behavior in npy_divmod')

