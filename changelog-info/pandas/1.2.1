(1, 'As a result, bugs reported as fixed in pandas 1.2.0 related to inconsistent tick labeling in bar plots are again present (:issue:`26186` and :issue:`11465`)')

(3, '    In [5]: np.add(df1, df2)')

(3, 'change, but the above example of ``np.add(df1, df2)`` with non-aligned inputs')

(3, 'objects (eg ``np.add(s1, s2)``) already aligns and continues to do so.')

(3, '    In [7]: np.add(df1, np.asarray(df2))')

(3, '    In [9]: np.add(df1, df2)')

(1, '.. _whatsnew_121.bug_fixes:')

(1, 'Bug fixes')

