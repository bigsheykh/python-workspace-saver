(1, 'NA values when the grouper was a categorical dtype. A fix for this was attempted in')

(1, 'that the patch fixing the ``dropna=False`` bug is incompatible with ``observed=False``,')

(1, '.. _whatsnew_151.bug_fixes:')

(1, 'Bug fixes')

