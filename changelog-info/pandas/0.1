(1, 'This is a minor release from 0.15.1 and includes a large number of bug fixes')

(1, 'A small number of API changes were necessary to fix existing bugs.')

(1, '- :ref:`Bug Fixes <whatsnew_0152.bug_fixes>`')

(1, '.. _whatsnew_0152.bug_fixes:')

(1, 'Bug fixes')

(3, '- Bug in outputting a MultiIndex with ``to_html,index=False`` which would add an extra column (:issue:`8452`)')

(1, '- BUG CSV: fix problem with trailing white space in skipped rows, (:issue:`8679`), (:issue:`8661`), (:issue:`8983`)')

