(3, '- Fixed regression in addition or subtraction of :class:`DateOffset` objects with millisecond components to ``datetime64`` :class:`Index`, :class:`Series`, or :class:`DataFrame` (:issue:`57529`)')

(1, '.. _whatsnew_221.bug_fixes:')

(1, 'Bug fixes')

(2, '    installed has been removed. This decision was made because the warning was too noisy for too')

(3, '    dependency. Pandas is currently considering the decision whether or not PyArrow should be added')

