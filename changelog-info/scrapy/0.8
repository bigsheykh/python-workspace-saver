(3, '   - added ``StatsCollector.iter_spider_stats()`` method')

(2, '   - removed ``StatsCollector.list_domains()`` method')

(2, '- HTTP Cache middleware has been heavily refactored, retaining the same functionality except for the domain sectorization which was removed. (:rev:`1843` )')

