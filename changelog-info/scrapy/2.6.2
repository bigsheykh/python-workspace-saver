(1, '**Security bug fix:**')

(2, '        without credentials, the ``Proxy-Authorization`` header is removed')

(2, '        removed.')

(1, 'Also fixes the following regressions introduced in 2.6.0:')

